#include <stdio.h>
#include <sys/types.h>
#include <sys/types.h>

int main(int argc, char const *argv[]) {
  int lez;
  int nEs;
  scanf("%d", &lez);
  mkdir(("/home/simone/projects/UNIPI/lez%d", lez), 0644);
  for(int i = 1; i <= nEs; i++) {
  }
  return 0;
}
